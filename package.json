{
  "name": "pincerx",
  "version": "1.0.0",
  "description": "PincerX API layer powered by OpenClaw core engine",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "test": "jest --coverage --verbose",
    "test:report": "jest --coverage --verbose --coverageReporters=text --coverageReporters=json-summary"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "openclaw/**/*.js",
      "api/**/*.js"
    ]
  },
  "dependencies": {
    "express": "^4.18.2",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
